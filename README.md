# Navega a la carpeta del proyecto
cd expol-marketplace
# Inicializa el repositorio Git
git init
# Agrega todos los archivos
git add ..
# Crea el primer commit
git commit -m "Initial commit: Project structure and setup"
# Conecta con el repositorio remoto (reemplaza con tu URL)
git remote add origin https://github.com/TU-USUARIO/expol-marketplace.git
# Verifica que el remote esté configurado
git remote -v
# Sube los archivos a GitHub
git branch -M main
git push -u origin main